# Deprecated — use Trinity instead

This command has been removed. To build a new pipeline, address Trinity directly in the chat:

```
Trinity, I need a <name> pipeline that <does X with tool Y>.
```

See `.claude/commands/README.md` for full details.
